name: CI/CD Pipeline Entrega

on:
  push:
    branches: [ "main", "dev2" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # UN SOLO JOB PARA TODO: Preparación y Validación
  build-and-validate:
    runs-on: ubuntu-latest

    steps:
    - name: 1. Descargar Código
      uses: actions/checkout@v4

    - name: 2. Configurar Entorno PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'

    - name: 3. Instalar Dependencias (Build)
      working-directory: ./backend
      run: composer install --no-ansi --no-interaction --no-scripts --prefer-dist

    - name: 4. Validación de Sintaxis (Linting)
      working-directory: ./backend
      run: |
        echo "Validando que no haya errores de sintaxis en el código PHP..."
<<<<<<< HEAD
<<<<<<< HEAD
        find . -name "*.php" -not -path "./vendor/*" | xargs -n 1 -P 4 php -l
=======
        find . -name ".php" -not -path "./vendor/" | xargs -n 1 -P 4 php -l
>>>>>>> bc2d029c4518308b84fe2aa5ff21c2b297cb2d82
=======
        find . -name "*.php" -not -path "./vendor/*" | xargs -n 1 -P 4 php -l
>>>>>>> 97df84729588304a933b4cfceb71d899b20a4d08
        echo "✅ Sintaxis validada correctamente."

    - name: 5. Simular Despliegue
      run: |
        echo "🚀 El entorno está listo."
        echo "📦 Código empaquetado y enviado al servidor de producción."
        echo "✅ Despliegue finalizado con éxito."